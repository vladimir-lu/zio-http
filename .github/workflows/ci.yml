# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['**']
    types: [opened, synchronize, reopened, edited]
  push:
    branches: ['**']
    tags: [v*]

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.12.15, 2.13.8, 3.1.1]
        java: [graal_21.1.0@11, temurin@8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup GraalVM (graal_21.1.0@11)
        if: matrix.java == 'graal_21.1.0@11'
        uses: DeLaGuardo/setup-graalvm@5.0
        with:
          graalvm: 21.1.0
          java: java11

      - name: Setup Java (temurin@8)
        if: matrix.java == 'temurin@8'
        uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Cache sbt
        uses: actions/cache@v2
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Check formatting
        if: matrix.scala == '2.13.8'
        run: sbt ++2.13.8 fmtCheck

      - name: Check that workflows are up to date
        run: sbt ++${{ matrix.scala }} githubWorkflowCheck

      - name: Build project
        run: sbt ++${{ matrix.scala }} test

      - name: Check doc generation
        if: ${{ github.event_name == 'pull_request' }}
        run: sbt ++2.13.8 doc

      - name: Compress target directories
        run: tar cf targets.tar target zio-http-test/target zio-http/target zio-http-benchmarks/target example/target project/target

      - name: Upload target directories
        uses: actions/upload-artifact@v2
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}
          path: targets.tar

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.8]
        java: [graal_21.1.0@11]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (full)
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Setup GraalVM (graal_21.1.0@11)
        if: matrix.java == 'graal_21.1.0@11'
        uses: DeLaGuardo/setup-graalvm@5.0
        with:
          graalvm: 21.1.0
          java: java11

      - name: Setup Java (temurin@8)
        if: matrix.java == 'temurin@8'
        uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: Cache sbt
        uses: actions/cache@v2
        with:
          path: |
            ~/.sbt
            ~/.ivy2/cache
            ~/.coursier/cache/v1
            ~/.cache/coursier/v1
            ~/AppData/Local/Coursier/Cache/v1
            ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-cache-v2-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Download target directories (2.12.15)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-2.12.15-${{ matrix.java }}

      - name: Inflate target directories (2.12.15)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (2.13.8)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-2.13.8-${{ matrix.java }}

      - name: Inflate target directories (2.13.8)
        run: |
          tar xf targets.tar
          rm targets.tar

      - name: Download target directories (3.1.1)
        uses: actions/download-artifact@v2
        with:
          name: target-${{ matrix.os }}-3.1.1-${{ matrix.java }}

      - name: Inflate target directories (3.1.1)
        run: |
          tar xf targets.tar
          rm targets.tar

      - env:
          PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
          CI_SONATYPE_RELEASE: ${{ secrets.CI_SONATYPE_RELEASE }}
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
        run: sbt ++${{ matrix.scala }} ci-release

  run_Jmh_BenchMarks:
    name: Jmh_Benchmarks
    strategy:
      matrix:
        os: [centos]
        scala: [2.13.8]
        java: [temurin@11]
    runs-on: [ "${{ matrix.os }}", zio-http ]
    steps:
      - uses: actions/setup-java@v2
        with:
          distribution: temurin
          java-version: 8

      - name: result_CookieDecodeBenchmark
        id: result_CookieDecodeBenchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          sbt -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 CookieDecodeBenchmark" | tee result_CookieDecodeBenchmark
          RESULT_CookieDecodeBenchmark=$(echo $(grep "thrpt" result_CookieDecodeBenchmark))
          echo "$RESULT_REQUEST_CookieDecodeBenchmark"
          IFS=' ' read -ra PARSED_RESULT <<< "$RESULT"
          echo ::set-output name=benchmark_result_CookieDecodeBenchmark::$(echo ${PARSED_RESULT[1]}": "${PARSED_RESULT[4]})

      - name: result_HttpCollectEval
        id: result_HttpCollectEval
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          sbt -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCollectEval" | tee result_HttpCollectEval
          RESULT_HttpCollectEval=$(echo $(grep "thrpt" result_HttpCollectEval))
          echo "$RESULT_REQUEST_HttpCollectEval"
          IFS=' ' read -ra PARSED_RESULT <<< "$RESULT"
          echo ::set-output name=benchmark_result_HttpCollectEval::$(echo ${PARSED_RESULT[1]}": "${PARSED_RESULT[4]})

      - name: result_HttpCombineEval
        id: result_HttpCombineEval
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          sbt -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpCombineEval" | tee result_HttpCombineEval
          RESULT_HttpCombineEval=$(echo $(grep "thrpt" result_HttpCombineEval))
          echo "$RESULT_REQUEST_HttpCombineEval"
          IFS=' ' read -ra PARSED_RESULT <<< "$RESULT"
          echo ::set-output name=benchmark_result_HttpCombineEval::$(echo ${PARSED_RESULT[1]}": "${PARSED_RESULT[4]})

      - name: result_HttpNestedFlatMapEval
        id: result_HttpNestedFlatMapEval
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          sbt -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpNestedFlatMapEval" | tee result_HttpNestedFlatMapEval
          RESULT_HttpNestedFlatMapEval=$(echo $(grep "thrpt" result_HttpNestedFlatMapEval))
          echo "$RESULT_REQUEST_HttpNestedFlatMapEval"
          IFS=' ' read -ra PARSED_RESULT <<< "$RESULT"
          echo ::set-output name=benchmark_result_HttpNestedFlatMapEval::$(echo ${PARSED_RESULT[1]}": "${PARSED_RESULT[4]})

      - name: result_HttpRouteTextPerf
        id: result_HttpRouteTextPerf
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          sbt -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 HttpRouteTextPerf" | tee result_HttpRouteTextPerf
          RESULT_HttpRouteTextPerf=$(echo $(grep "thrpt" result_HttpRouteTextPerf))
          echo "$RESULT_REQUEST_HttpRouteTextPerf"
          IFS=' ' read -ra PARSED_RESULT <<< "$RESULT"
          echo ::set-output name=benchmark_result_HttpRouteTextPerf::$(echo ${PARSED_RESULT[1]}": "${PARSED_RESULT[4]})

      - name: result_ProbeContentTypeBenchmark
        id: result_ProbeContentTypeBenchmark
        env:
          GITHUB_TOKEN: ${{secrets.ACTIONS_PAT}}
        run: |
          cd zio-http
          sed -i -e '$aaddSbtPlugin("pl.project13.scala" % "sbt-jmh" % "0.4.3")' project/plugins.sbt
          sbt -v "zhttpBenchmarks/jmh:run -i 3 -wi 3 -f1 -t1 ProbeContentTypeBenchmark" | tee result_ProbeContentTypeBenchmark
          RESULT_ProbeContentTypeBenchmark=$(echo $(grep "thrpt" result_ProbeContentTypeBenchmark))
          echo "$RESULT_REQUEST_ProbeContentTypeBenchmark"
          IFS=' ' read -ra PARSED_RESULT <<< "$RESULT"
          echo ::set-output name=benchmark_result_ProbeContentTypeBenchmark::$(echo ${PARSED_RESULT[1]}": "${PARSED_RESULT[4]})

      - uses: peter-evans/commit-comment@v1
        with:
          sha: ${{github.sha}}
          body: |

            **🚀 Jmh Benchmark:**

            ${{steps.result_CookieDecodeBenchmark.outputs.benchmark_result_CookieDecodeBenchmark}}
            ${{steps.result_HttpCollectEval.outputs.benchmark_result_HttpCollectEval}}
            ${{steps.result_HttpCombineEval.outputs.benchmark_result_HttpCombineEval}}
            ${{steps.result_HttpNestedFlatMapEval.outputs.benchmark_result_HttpNestedFlatMapEval}}
            ${{steps.result_HttpRouteTextPerf.outputs.benchmark_result_HttpRouteTextPerf}}
            ${{steps.result_ProbeContentTypeBenchmark.outputs.benchmark_result_ProbeContentTypeBenchmark}}
